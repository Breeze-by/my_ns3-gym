# 2026-09-23 P3A.5 当前 task-stack 重验证

## 结论

本轮在冻结候选提交 `41f63fb16d64e21a3ae1f296d509303b96b74528` 上运行了完整的 P2D/P3A
理想任务矩阵和强制充电回归。P3A.5 **未通过，待修复后重跑**：10 个正式 episode 中
9 个以 `COMPLETE` 结束，1 个为启动后任务失败；没有基础设施失败，也没有碰撞。旁路审计
子门通过，因此失败原因是任务栈的 rally 导航鲁棒性，不是通信路径绕过或启动不完整。

P3B 尚未开始。`task_stack_frozen_commit` 不能在本轮作为可冻结基线使用，直到 lab/seed202
的启动后失败被定位并按同一规则重跑成功。失败 episode 保留，不用重试结果替换。

## 冻结内容和验证

正式 runner 为 `scripts/run_p2d_baseline.py`，固定场景配置为：lab `(-4,4,40)`、rooms
`(5,3,40)`、corridors `(-4.5,-0.5,45)`，三机器人 seeds `101/202/303`，另加走廊双机器人
seed 202 交叉检查。runner 使用启动超时 600 s、消息超时 90 s、评估等待 600 s、任务时长
300 s 和 ROS domain 220–229；仅在“没有评估结果且 episode 尚未开始”时保留基础设施重试。

执行前完成了 `multi_robot_interfaces`、`multi_robot_exploration`、`merge_map` 的
`colcon build --symlink-install`，13 个 gateway/evaluator 单元测试通过，源码旁路审计通过。
正式批次的 summary 为：

```text
ros2_ws/ros2-multi-robot-automap/log/p2d_baseline/p3a5_current_head_41f63fb/summary.json
```

manifest 记录了干净工作树、提交哈希、场景/旁路清单/GatewayEnvelope/launch/源码/参数哈希，
以及 Python 3.10.12、ROS Humble、Gazebo 11.10.2 和 runner 参数。每个正式 episode 都保存了
启动日志、评估 JSON、尝试记录和 ROS graph 快照。

## 正式矩阵

| 场景 | 机器人 | seed | 结果 | 完成时间(s) | 发现(s) | rally(s) | 正确自由覆盖率 | 充电 | 碰撞 |
|---|---:|---:|---|---:|---:|---:|---:|---:|---:|
| lab | 3 | 101 | `COMPLETE` | 148.0 | 52.4 | 62.2 | 0.879 | 0 | 0 |
| lab | 3 | 202 | **timeout/RALLY** | — | 70.6 | 71.5 | 0.944 | 1 | 0 |
| lab | 3 | 303 | `COMPLETE` | 144.3 | 47.7 | 60.1 | 0.939 | 0 | 0 |
| rooms | 3 | 101 | `COMPLETE` | 131.6 | 72.7 | 73.6 | 0.971 | 0 | 0 |
| rooms | 3 | 202 | `COMPLETE` | 145.2 | 68.9 | 81.1 | 0.964 | 0 | 0 |
| rooms | 3 | 303 | `COMPLETE` | 101.8 | 42.3 | 53.8 | 0.805 | 0 | 0 |
| corridors | 3 | 101 | `COMPLETE` | 147.6 | 63.9 | 64.8 | 0.821 | 0 | 0 |
| corridors | 3 | 202 | `COMPLETE` | 129.7 | 47.6 | 59.6 | 0.817 | 0 | 0 |
| corridors | 3 | 303 | `COMPLETE` | 139.1 | 47.9 | 62.5 | 0.816 | 0 | 0 |
| corridors cross-check | 2 | 202 | `COMPLETE` | 157.2 | 59.9 | 70.3 | 0.809 | 0 | 0 |

9/10 的 `success=true`、`termination_reason=task_complete`、`task_phase=COMPLETE`，10/10
`collision_events=0`，10/10 `infrastructure_failure=false`。唯一失败的 episode 已开始并产生
完整评估结果，所以按协议计为任务失败，不补跑替换。

## 失败定位

失败文件为：

```text
ros2_ws/ros2-multi-robot-automap/log/p2d_baseline/p3a5_current_head_41f63fb/episodes/p3a5_current_head_41f63fb_lab_far_northwest_3r_seed202.json
```

它在 `RALLY` 阶段运行到 300.4 s，`tb3` 发生一次安全返航和充电，随后恢复任务；最终
`tb3` 的 rally 误差为 5.78 m，协调器在超时后取消目标。启动日志记录了局部/全局规划的
`Starting point in lethal space`、`No valid trajectories` 和控制器 `Failed to make progress`。
该证据支持“充电恢复后的 rally 路径/代价图或进展处理存在 seed 相关脆弱性”的定位假设，
但还不足以断言单一根因。下一轮应先复现同一 seed，核对返航前后地图版本、机器人位姿、
全局代价图输入和 rally 目标；修复后仍须完整重跑，不得只重跑这一格并将其替代原失败记录。

## 强制充电回归

独立的 2 机器人、seed 303、初始能量 18 回归通过：

```text
ros2_ws/ros2-multi-robot-automap/log/p2d_baseline/p3a5_current_head_41f63fb/forced_charge/p3a5_current_head_41f63fb_forced_charge_2r_seed303.json
```

结果为 `COMPLETE`，两台机器人各完成 1 次充电（总计 2 次），总充电时间 32.9 s，最低电量
6.49，`collision_events=0`。这证明当前 HEAD 的安全余量返航和充电恢复路径可运行，但不
能覆盖失败 seed 的 rally 稳定性问题。

## ROS graph 和旁路审计

10 个正式快照和强制充电快照共 11 份，均由 smoke 的 P3A audit 通过。独立复核确认：

- `/headquarters_control` 只订阅 `/gateway/received/...`，Nav2 action client 只指向
  `/gateway/tbN/navigate_to_pose`；
- `/merge_map` 只消费 `/gateway/received/tbN/map`；
- 每个机器人全局代价图只消费 `/tbN/gateway/merge_map`，没有直接订阅 `/merge_map`；
- 中央节点没有 `/tbN/map`、`/tbN/odom`、`/tbN/tf` 或直接 `/tbN/navigate_to_pose` action
  的旁路。

`/ideal_gateway` 订阅机器人原始输入并向 gateway 接收主题发布，这是通信边界本身的允许行为。
`/target_detector` 和 `task_evaluator` 对 Gazebo 真值、原始检测、原始碰撞和 action status 的
订阅是明确的 evaluator/truth 例外，不属于控制链；中央控制器仍只消费 gateway 交付的检测和
状态。

## 路线调整

P3A.5 的协议和图边界已经得到当前 HEAD 的运行时证据，但完整任务门禁尚未闭合。下一步顺序
固定为：

1. 保留本次 9/10 原始批次，分析 lab/seed202 的返航后 rally 失败；必要时增加只读诊断字段，
   不改变成功规则；
2. 在修复提交上按同样的 10 格矩阵和强制充电命令重跑，生成新的 manifest 和报告；
3. 只有 10/10 `COMPLETE`、零碰撞、零基础设施失败且图审计通过，才把该提交冻结为
   `task_stack_frozen_commit`，再进入 P3B；
4. P3B 仍按已修订路线先做固定 delay/loss、stale-state、ACK/retry 和消息账本，不提前接
   ns-3 或训练 RL。

## 诊断与保守修复（2026-09-23）

对 lab/seed202 的失败格进行了同 seed 诊断。原始正式运行在充电恢复后的 rally 阶段出现
`Starting point in lethal space`、`No valid trajectories` 和 `Failed to make progress`，但
没有碰撞或基础设施失败。日志中还能看到控制器在地图路径不可用时继续发送最终 rally pose。

源码检查确认 `plan_rally_leg()` 的旧回退会在起点未知或无路由时直接返回当前位置到最终 pose
的伪路径，绕过 `path_waypoint_route()`；路径函数本身也没有拒绝越界或不可通行目标格。这是
一个真实的安全性和可观测性缺陷，但它还不是 Nav2 报告的全部根因，因为失败格也出现了
局部 costmap 对实际起点判为 lethal 的情况。

修复提交中的最小改动是：路径函数先做边界/可通行校验；无安全路径时返回空计划；调度器在
现有等待窗口后报告 `rally_route_unavailable:<robot>` 并结束任务；新增断开地图和越界目标的
单元测试。构建、43 个定向 pytest 和源码旁路审计均通过。

同 seed 的修复诊断结果保存在
`ros2_ws/ros2-multi-robot-automap/log/p2d_baseline/p3a5_diag_route_fix_41f63fb/`，结果是
`mission_failed`、`rally_route_unavailable:tb2`、零碰撞、零基础设施失败。这个结果说明故障
现在被显式拒绝和记录，不能当作 P3A.5 成功；本报告的正式结论仍是 9/10，P3A.5 未通过，
P3B 未开始。

下一轮需要在不改变成功语义的前提下补充：地图序号和时间、合并地图 origin/resolution、
机器人位姿、局部/全局 costmap 更新时间、planner 起点栅格状态，以及充电恢复到首次 rally
派发之间的时间间隔。只有据此确认地图/costmap 的时序或坐标根因，并在修复提交上重新跑完整
10 格矩阵后，才可以更新冻结基线。

## 路径守卫提交的完整重跑（2026-09-23）

提交 `c369c7d37fdbad5be5e13ad13f7626007212d407` 在干净工作树上完成了同口径的 10 格矩阵，
输出目录为 `ros2_ws/ros2-multi-robot-automap/log/p2d_baseline/p3a5_route_guard_c369c7d/`。
结果为 8/10 `COMPLETE`、10/10 零碰撞、0/10 基础设施失败。lab/seed202 和 rooms/seed303
均在 `EXPLORE` 阶段超时；两格都已经启动并保存评估 JSON，所以按规则保留为任务失败。

这次重跑说明路径守卫自身没有破坏已通过的场景，也没有解决完整任务稳定性。lab/seed202 的
启动日志仍出现 `Starting point in lethal space` 与 tb2 `Failed to make progress`，rooms/seed303
则在一次充电恢复后没有进入目标确认。P3A.5 仍未通过，不能冻结该提交，P3B 仍未开始。

当前 graph 快照显示三个 global costmap 都订阅 `/tbN/gateway/merge_map`，中央控制器和 Nav2
目标仍经过 gateway。需要避免把“中央地图规划有路径”当成“Nav2 起点一定可用”：中央
`nearest_traversable()` 会在有限半径内吸附起点，而 Nav2 还会叠加自身的 global/local costmap、
激光 obstacle layer 和 TF 时序。这个差异是待验证诊断方向，不是已证实的单一根因。

下一步不改变任务成功语义，先加入只读诊断字段：map/gateway merge 序号和时间、origin/
resolution、map→odom 机器人位姿、global/local costmap 更新时间、实际起点栅格代价，以及
planner 失败时的最近快照。只在 lab/seed202 与 rooms/seed303 各复现并比较这些字段，确认
时序/坐标/dynamic obstacle 根因后，再选择等待新地图、清理 costmap 或收紧中央起点吸附半径；
之后才值得重新跑完整矩阵。

## 针对 lab/seed202 的修复验证（2026-09-23，非正式矩阵）

在提交 `c369c7d` 的失败路径上，日志显示 `tb3` 在一个集合位姿已到位后暂时没有中央地图
安全路线；固定的 10 秒窗口直接把任务标为 `rally_route_unavailable`。这不是 gateway
消息丢失：目标已通过 `/gateway/received/target_detection` 到达总部，其他机器人也继续收到
位姿、地图和导航结果。失败同时暴露了探索路径规划把实际位于障碍膨胀区的机器人起点吸附
到附近自由格的问题，这会使中央路线看似可行而 Nav2 报 `Starting point in lethal space`。

当前修复包含三部分：

1. 探索规划要求收到的地图把机器人实际起点标为安全格；不再把真实起点吸附到邻近格，也不
   在没有安全起点时发送 frontier 目标；
2. RALLY 路径不可用时保留显式失败守卫，但等待窗口增至 30 秒；等待超过 2 秒后基于当前
   融合地图重新选择可达、互相分离的集合位姿，并通过同一 gateway 发布新的集合分配；
3. 新增对应单元测试，覆盖不安全实际起点和避开已占集合位姿的重新分配。

同一 `my_world.world`、3 机器人、Gazebo seed 202 的定向重跑结果为：`COMPLETE`，162.9 s，
零碰撞、零导航 abort、39/42 导航目标成功，最终集合位姿最小间距 1.57 m。日志仍可见 Nav2
在一次动态起点恢复期间报告 lethal，但协调器最终完成集合；这说明修复消除了当前失败门，
但不等于 Nav2 costmap 时序问题已经完全解释。47 个定向 pytest 和两个 ROS 包构建通过。
该结果只作为诊断证据，不能替换失败批次；必须在修复提交上完整重跑 10 格矩阵后再判断
P3A.5 是否通过并冻结 `task_stack_frozen_commit`。


## 起点收紧后的调度死锁定位与串行验证（2026-09-23）

上一节所述 162.9 s 定向结果没有触发 `Reassigned` 分支，不能证明重新分配已经生效；现已明确标注为候选起点修复的诊断结果。将 rally 分配、重新分配和逐段规划统一改为要求机器人真实位置对应的栅格可通行，重新运行 lab/seed202 后，tb3 能够完成多段路径，但在 tb1/tb2 已到位后，它们的动态安全膨胀区切断了 tb3 的剩余路径，最终仍为 `rally_route_unavailable:tb3`。

因此将 `RALLY_MAX_CONCURRENT` 从 2 改为 1，使已到位机器人不会成为尚未完成机器人路径上的动态障碍。相同 seed 的串行定向验证在 186.7 s 以 `COMPLETE` 结束，0 碰撞、0 导航 abort、0 充电，旁路审计通过。该结果支持“并发集合调度死锁”诊断，但仍不是正式门禁；必须在包含 lab/rooms/corridors 三 seed 和 corridors 双机器人交叉检查的 10 格矩阵上验证。


## 近侧优先与安全让路实验（2026-09-23）

串行 rally 的正式 10 格批次只得到 5/10：lab/seed202、rooms/seed303 和 corridors/seed101/202/cross-check 等格仍在到位机器人封路或 rally 超时处失败。将已到位机器人从栅格障碍中移除的实验产生 32 次碰撞，已明确撤回。

当前工作树改为近侧优先派发，并在某机器人无路由超过 2 秒时尝试让一个已到位机器人迁移到新的、与其它集合位保持间距的安全候选位；仍使用动态障碍膨胀、实际起点检查和活动路线冲突检查。lab/seed202 定向结果为 `COMPLETE`（140.7 s），0 碰撞、0 导航 abort、0 充电，旁路审计通过；本次样本未触发展开让路分支，因此不能替代正式矩阵。


## 可逆 parked-yield 扩展（2026-09-23）

近侧优先仍可能在目标区窄通道中互相封路；因此在无路由等待超过 2 秒后，为已到位机器人寻找临时安全栅格并让其移动，随后自动恢复原最终 rally 位姿。临时位选择保留路径清空、真实起点检查和 `RALLY_MIN_SEPARATION_M`，不移除动态障碍，也不允许穿越。新增 `rally_yield_pose` 单元测试，控制器测试 34 项通过。

该改动的定向运行受到先前 Gazebo/Nav2 lifecycle 残留影响而中断，尚无可作为门禁的运行结果；正在运行的 `p3a5_yield_0541ce7` 正式矩阵仍按上一提交执行，结果需单独记录。


## probe-action 定向修复（2026-09-23）

`p3a5_yield_0541ce7` 正式矩阵为 6/10，0 基础设施失败；失败包含 corridors/seed202 三机器人和双机器人路由不可用，以及 rooms/seed202 的 14 碰撞。b1a4d7c 的 parked-yield 已能移动并恢复阻塞机器人，但三机器人窄走廊仍可能使当前机器人自身无路由。

修复 probe 状态机：复用已有 `send_survey_goal` action 发送短 survey 位，action 成功后恢复 `rally_final_targets`；不再每个 timer 重写 probe 目标。当前工作树定向结果：rooms/seed202 `COMPLETE`（144.4 s，0 碰撞、0 nav abort）；corridors/seed202 双机器人 `COMPLETE`（119.8 s，0 碰撞、0 nav abort）；corridors/seed202 三机器人 `COMPLETE`（157.3 s，0 碰撞、1 nav abort），日志明确记录 parked-yield、probe 和最终位姿恢复。完整 10 格矩阵仍需在新提交上重跑。

## probe-action 提交的正式矩阵（2026-09-23）

在干净的 `04e684d` 提交上执行与上节相同的 `run_p2d_baseline.py` 十格门禁（`--run-id p3a5_probe_04e684d --ros-domain-base 210`，其余参数见 `log.md`）。`summary.json` 记录 8/10 成功、0 基础设施失败，10 份 graph 快照的启动审计全部通过。三世界九格分别是 lab 2/3、rooms 3/3、corridors 2/3；corridors 双机器人交叉格通过。lab/seed202 在 `RALLY` 超时且无碰撞，日志表明 tb2 的 probe 恢复最终位后触发安全返航、tb1 随后返航。corridors/seed101 虽达 `COMPLETE`，tb1 有 3 次碰撞，故仍为任务失败。其余 8 格都是零碰撞 `COMPLETE`。该结果不能满足 P3A.5 退出条件；`task_stack_frozen_commit` 仍未设置，P3B 未开始。

## 恢复状态机定向验证（2026-09-23）

针对 corridors/seed101 日志中 tb3 连续 Nav2 abort 后继续沿旧动态路线重试的问题，增加连续两次 rally action 失败后的强制一次恢复；若 parked robot 被重新分配到有效安全集合位，则保留新位为最终目标；probe action 期间阻止同机器人普通 rally 并发。构建及 49 项测试通过。

lab/seed202 在第一次定向启动误用了 `ROS_DOMAIN_ID=240`，Fast DDS 因 domain 超过 232 提前退出，未产生 episode；改用 230 后以 `COMPLETE` 结束（169.5 s，0 碰撞、0 nav abort、0 充电）。corridors/seed101 的第一次迭代以 300.4 s 超时但 0 碰撞，tb3 在评估保存后才到位；启用恢复状态并以 domain 228 重跑后 `COMPLETE`（159.5 s，0 碰撞、0 nav abort、0 充电）。三次均通过旁路审计。结果只证明定向路径改善，不能替代下一步完整 10 格门禁。

## `11e0138` 完整矩阵结果（2026-09-23）

在已推送的 `11e0138` 上按原协议完成 10 格矩阵，输出目录为 `ros2_ws/ros2-multi-robot-automap/log/p2d_baseline/p3a5_recovery_11e0138/`。结果为 5/10 成功、0 基础设施失败，10/10 graph 审计通过。成功格为 lab/303、rooms/101、rooms/202、corridors/101 和 corridors 双机器人交叉格；lab/101 在 `FOUND` 超时，lab/202 在 `RALLY` 超时，rooms/303 在 `RALLY` 发生 13 次碰撞并超时，corridors/202 三机器人在 `RALLY` 超时，corridors/303 在 `RALLY` 发生 14 次碰撞并超时。P3A.5 仍未通过，不能设置 `task_stack_frozen_commit`，P3B 不开始。新失败分布说明单次 action 失败恢复已改善 corridors/101，但没有解决目标区动态拥塞和部分 seed 的前置探索/电池时序问题；下一步应分析失败日志中的首个碰撞、目标区路线和电池恢复顺序，不能用定向成功替换正式失败格。

## Rally 屏障和受阻机器人重分配（2026-09-23）

正式批次的 rooms/303、corridors/303 日志显示 parked-yield 机器人刚到临时位就恢复旧最终位，和仍在移动的机器人重新争用窄通道。当前修复让临时位成为阶段屏障：让路机器人保持在安全位，暂停其它 rally 派发，待其余机器人完成后再恢复；若受阻机器人仍没有中央安全路线，则按相同净空、实际起点和最小间距约束重新选择一个可达最终位。

构建和 49 项测试通过。rooms/303 定向迭代 `p3a5_iter3_rooms303` 为 `COMPLETE`、124.5 s、0 碰撞、0 nav abort。corridors/303 的第一版屏障 `p3a5_iter3_corridors303` 以 0 碰撞但 RALLY 超时结束，证明安全屏障避免了碰撞但会等待死锁；加入受阻机器人重分配后 `p3a5_iter4_corridors303` `COMPLETE`、128.2 s、0 碰撞、0 nav abort。三次 graph 审计均通过。该修复尚未形成新的完整 10 格门禁，不能更新冻结基线。

## 2026-09-24 survey 轮转与 probe 超时定向回归

在 `e551a22` 的 rally 屏障/受阻机器人重分配基础上，修复 FOUND 阶段 survey robot 轮转和 RALLY probe action 超时清理。新增 `rotate_robot_order` 测试；构建及四个 ROS 测试包共 50 项通过。

corridors 定向结果：

| Run | Robots / seed | Result | Elapsed | Collisions | Nav aborts | Charges |
|---|---:|---|---:|---:|---:|---:|
| `p3a5_iter5_corridors202` | 3 / 202 | `COMPLETE` | 167.4 s | 0 | 0 | 0 |
| `p3a5_iter5_corridors303` | 3 / 303 | `COMPLETE` | 170.9 s | 0 | 0 | 0 |

两份 graph 快照均通过 P3A forbidden-bypass 审计。seed303 日志确认 probe、临时让位、临时位屏障和最终 rally pose 恢复链路全部执行。该结果只证明定向回归有效；`p3a5_barrier_e551a22` 的正式矩阵仍为 8/10，必须在新提交上重跑完整 10 格门禁。

## 2026-09-24 正式矩阵中断与控制流修复

在 `75907b7` 上启动 `p3a5_formal_75907b7`。第一格 lab/seed101 保存为 `insufficient_rally_poses`；第二格 lab/seed202 触发受阻机器人重分配后的 `TypeError`（`rally_route_unavailable_since` 被清空后同一 timer 继续做时间差计算），因此中断批次，不能计入门禁。输出保留在 `log/p2d_baseline/p3a5_formal_75907b7/`。

修复已将重分配后的不可用时间戳设为当前时间，保留恢复状态并消除 `None` 差值路径；重新构建和四个 ROS 测试包共 50 项通过。下一批正式矩阵必须使用修复后的新提交。

## 2026-09-24 电池屏障与串行 rally 诊断

`p3a5_iter6_lab101` 在 RALLY 期间发生 24 次碰撞，日志确认 tb2 返航而其它 rally legs 未暂停；`p3a5_iter7_lab101` 加入全局电池屏障后碰撞为 0，但一次充电使 RALLY 到 285.4 s 才开始，300.4 s 超时。调整 survey 轮转为仅失败时前进后，`p3a5_iter8_lab101` 快速进入 RALLY，但两条并发 rally 路线造成 24 次碰撞。恢复 `RALLY_MAX_CONCURRENT=1` 并完成 51 项测试；随后 `p3a5_iter9_lab101` 因 Gazebo exit 255 启动故障中断，没有 episode 结果。上述结果用于定位电池/动态路线时序，不构成 P3A.5 通过证据。

## 2026-09-24 串行 rally 与电池屏障定向通过

在 `f7a9b11` 上干净重跑 `p3a5_iter10_lab101`：`COMPLETE`，162.6 s，0 碰撞、0 nav abort、0 充电；目标检测 72.6 s、RALLY 80.6 s，graph 旁路审计通过。该结果支持当前组合修复，但不能替代完整 10 格正式门禁。

## 2026-09-24 `e6267d9` 正式矩阵及后续定向诊断

`p3a5_formal_e6267d9` 按完整 10 格协议执行（准确命令见 `log.md`），结果 5/10 成功、0 基础设施失败；10 份 ROS graph 均保存，episode 启动时的 P3A forbidden-bypass 审计均通过。成功格为 rooms/101、202、303，corridors/101 和 corridors 双机器人 seed202。失败格：lab/101 `COMPLETE` 但 2 次碰撞；lab/202 `COMPLETE`、0 碰撞，但 tb3 最终角速度 0.202 rad/s 超过 0.1 rad/s 容差；lab/303 `RALLY` 超时、0 碰撞；corridors/202 `RALLY` 超时、0 碰撞；corridors/303 `RALLY` 超时且 23 次碰撞。输出位于 `log/p2d_baseline/p3a5_formal_e6267d9/`。P3A.5 仍未通过，不能冻结提交或进入 P3B。

在 `e6267d9` 加本地动态阻挡/全局 survey 屏障改动上，构建与 51 项测试通过。`p3a5_iter11_corridors303` 定向完成于 170.0 s，0 碰撞、0 nav abort、0 充电，旁路审计通过；`p3a5_iter12_lab101` 却在 `RALLY` 300 s 超时、0 碰撞。该次 lab 日志显示 tb3 probe 起点被 Nav2 判为 lethal，probe 与 tb2 rally leg 重叠并重复触发恢复，tb2 到结束仍离目标集合位 5.59 m。两次结果都已记录在各自输出目录；这组定向证据不足以更新正式门禁。

补充定向记录：动态当前位置阻挡加入回退到已到位机器人阻挡后，`p3a5_iter14_lab101` 仍在 `RALLY` 超时（0 碰撞、0 nav abort、0 充电，旁路审计通过）。该结果说明 lab/101 还存在集合路由活性问题，不能用 corridors/303 的定向通过替代完整门禁。
